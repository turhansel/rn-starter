import React from 'react';
import { Text, StyleSheet, View } from 'react-native';

const ImageDetails = () => {
	return <View>ImageDetails</View>;
};

const styles = StyleSheet.create({
	text: {
		fontSize: 30,
	},
});

export default ImageDetails;
